let {addTask} = require('../../lib/agenda_helper')
// let {sync_once} = require('./c')
async function init (){
  // addTask('1 hours', 'coins_sync', async () => {
  //   await sync_once()
  // })
}

module.exports = {
  init
}